package com;

import money.NeedService;
import org.junit.Assert;
import org.junit.Test;

public class NeedServiceTest {

    NeedService service = new NeedService();
@Test
    public  void  v(){
    service.create(null);

    Assert.assertTrue(false);

}
}
